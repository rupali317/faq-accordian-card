function toggleStates(id) {
    /*
    add clicked status if there is no clicked status to that id, transform arrow, show answer,change qs color to dark
    remove clicked status if there is clicked status to that id, transform arrow, hide answer,change qs color to grey
    */
   
}